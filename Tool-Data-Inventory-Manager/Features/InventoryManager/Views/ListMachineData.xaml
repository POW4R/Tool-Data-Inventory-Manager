<Window x:Class="Tool_Data_Inventory_Manager.Features.InventoryManager.Views.ListMachineData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tool_Data_Inventory_Manager.Features.InventoryManager.Views"
        mc:Ignorable="d"
        Title="ListMachineData" Height="450" Width="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- ToolId -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,0,0,10">
            <TextBlock Text="ToolId:"
                       FontWeight="Medium"
                       Width="150"
                       Foreground="{DynamicResource MaterialDesignBody}" />
        </StackPanel>

        <!-- MaterialNumber -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="20,0,0,10">
            <TextBlock Text="MaterialNumber:"
                       FontWeight="Medium"
                       Width="150"
                       Foreground="{DynamicResource MaterialDesignBody}" />
        </StackPanel>

        <!-- MagPlace (opcionális) -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="20,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="MagPlace (opcional):"
                       FontWeight="Medium"
                       Width="150"
                       Foreground="{DynamicResource MaterialDesignBody}" />
            
        </StackPanel>
        <StackPanel Grid.Row="4" HorizontalAlignment="Right" Orientation="Horizontal" Margin="10,240,0,10" VerticalAlignment="Bottom">
            <Button x:Name="btn_add_magPlace"
                    Content="Add magPlace"
                    Margin="0,0,0,10"
                    Padding="10,5"
                    Click="btn_add_magPlace_Click"/>
            <Button x:Name="btn_Save"
                    Margin="10,0,0,10"
                    Content="Save"
                    Padding="10,5"
                    Click="btn_Save_Click"/>
            <Button x:Name="btn_Back"
                    Margin="10,0,0,10"
                    Content="Back"
                    Padding="10,5"
                    Click="btn_Back_Click"/>
        </StackPanel>
    </Grid>
</Window>
